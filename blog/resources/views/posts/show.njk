{% extends 'master' %}

{% block content %}
  <div class="post">
    <div>
      <a href="/">  Go Back </a>
    </div>
    <hr>
    <h2>{{ post.title }}</h2>
    <h4>{{ post.author }}</h4>
    <p>{{ post.content }}</p>
  </div>
{% endblock %}